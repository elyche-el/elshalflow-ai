import { NextRequest, NextResponse } from "next/server";import { getServiceClient } from "@/lib/supabase/anon-client";export async function GET(req:NextRequest){const url=new URL(req.url);const cid=url.searchParams.get("conversationId");if(!cid)return NextResponse.json({error:"conversationId required"},{status:400});const sb=getServiceClient();const{data}=await sb.from("anon_messages").select("*").eq("conversation_id",cid).order("created_at",{ascending:true}).limit(200);return NextResponse.json({messages:data||[]})}
