"use client";import { Sidebar } from "@/components/layout/sidebar";import { useState, useEffect } from "react";export function DashboardLayout({ children }: { children: React.ReactNode }) { const [convId, setConvId] = useState<string>(""); useEffect(() => { const params = new URLSearchParams(window.location.search); const c = params.get("c"); if (c) setConvId(c); }, []); function handleSelect(id: string) { setConvId(id); window.history.pushState({}, "", "/chat?c=" + id); } function handleNew() { setConvId(""); window.history.pushState({}, "", "/chat"); } return (<div className="flex h-screen overflow-hidden bg-background"><Sidebar activeConvId={convId} onSelect={handleSelect} onNew={handleNew} onDelete={(id: string) => { if (convId === id) handleNew(); }} /><main className="flex-1 overflow-hidden">{children}</main></div>); }
