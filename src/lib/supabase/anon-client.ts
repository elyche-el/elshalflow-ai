import { createClient } from "@supabase/supabase-js";const U=process.env.NEXT_PUBLIC_SUPABASE_URL||"";const AK=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||"";const SK=process.env.SUPABASE_SERVICE_ROLE_KEY||"";export function getServiceClient(){if(!U||!SK)throw new Error("Missing Supabase env vars");return createClient(U,SK,{auth:{autoRefreshToken:false,persistSession:false}})}export function getAnonClient(){return createClient(U,AK)}
